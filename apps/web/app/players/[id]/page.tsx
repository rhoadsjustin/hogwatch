import { notFound } from 'next/navigation';import { AskCard } from '../../../components/AskCard';import { getPlayer } from '../../../lib/data';
export default async function PlayerPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=getPlayer(id);if(!p)notFound();return <div className="shell"><a href="/">← Team</a><section className="detailHead"><span>{p.position} • #{p.number} • {p.classYear}</span><h1>{p.name}</h1><p>{p.height} • {p.weight} lbs • {p.hometown}</p></section><div className="metricgrid">{Object.entries(p.stats).map(([k,v])=><div className="metric" key={k}><span>{k}</span><b>{v}</b></div>)}</div><AskCard label={`Ask ChatGPT about ${p.name}`} /></div>}
